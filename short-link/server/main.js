import { Meteor } from 'meteor/meteor';
import '../imports/api/users';
//not getting exports so just importing file so it runs.

import '../imports/api/links';
import '../imports/startup/simple-schema-config.js';

Meteor.startup(() => {

});
